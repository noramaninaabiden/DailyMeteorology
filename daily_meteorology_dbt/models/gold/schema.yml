version: 2

models:
  - name: gold_city_daily
    columns:
      - name: city
        tests:
          - not_null
      - name: date
        tests:
          - not_null

    tests:
      - dbt_utils.unique_combination_of_columns:
          combination_of_columns:
            - city
            - date